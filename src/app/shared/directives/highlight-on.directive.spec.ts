import { HighlightOnDirective } from './highlight-on.directive';

describe('HighlightOnDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnDirective();
    expect(directive).toBeTruthy();
  });
});
